<nav class="navbar navbar-static-top navbar-default" role="navigation">
  <% if user_signed_in? %>
    <p class="navbar-text">
      <%= gravatar_for current_user.email, size: 32, class: "img-circle" %>
      <%= current_user.name %> | <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
    </p>
  <% end %>
</nav>